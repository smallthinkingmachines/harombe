# Harombe Docker Environment Configuration
# Copy this file to .env and customize for your setup

# Gateway Configuration
GATEWAY_HOST=0.0.0.0
GATEWAY_PORT=8100
LOG_LEVEL=INFO

# Volume Mounts
# Workspace directory (mounted read-only in filesystem container)
HAROMBE_WORKSPACE=./workspace

# Projects directory (mounted read-write in filesystem container)
HAROMBE_PROJECTS=./projects

# Container-specific Environment Variables

# Browser Container
# DISPLAY=:99  # X11 display for Xvfb

# Code Execution Container
# EXECUTION_TIMEOUT=30  # Timeout in seconds for code execution

# Web Search Container
# No additional configuration needed (uses DuckDuckGo API)
